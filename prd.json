{
  "project": "innfiswindow",
  "description": "Standalone Kubernetes resource management desktop application with tree navigation and resource views.",
  "tech_stack": {
    "framework": "Electron + React + TypeScript",
    "build_tool": "Vite (electron-vite)",
    "k8s_client": "@kubernetes/client-node",
    "ui_library": "shadcn/ui + Tailwind CSS v4",
    "state_management": "Zustand",
    "notes": "Use electron-vite for Electron + Vite integration. Tailwind v4 via @tailwindcss/vite plugin. shadcn/ui components copied into src/renderer/components/ui/. Main process handles kubeconfig and k8s API calls via IPC; renderer is the React UI."
  },
  "architecture": {
    "main_process": "Reads ~/.kube/config, creates k8s API clients, exposes IPC handlers for resource queries",
    "renderer_process": "React app with Tailwind layout: left tree view + right resource panel. shadcn/ui components for Table, Sheet, Badge, Collapsible.",
    "ipc_pattern": "renderer calls ipcRenderer.invoke('k8s:<resource>:<action>', ...args), main process returns serialized data"
  },
  "stories": [
    {
      "id": "S87",
      "title": "Cluster context alias: set and edit a display name per context",
      "status": "complete",
      "description": "Kubeconfig context names are often long, auto-generated strings (e.g. 'arn:aws:eks:us-east-1:123456789:cluster/prod-cluster'). Allow users to set a short human-readable alias for each context that replaces the raw name everywhere it is displayed in the UI — the AppBar context selector, the tree view cluster label, and any other place the context name appears. The raw context name is kept internally for all API calls; only display is affected.",
      "acceptance_criteria": [
        "Zustand store gains a persisted field: contextAliases: Record<string, string> (context name → alias); included in persist partialize",
        "AppBar context selector displays alias instead of raw context name when an alias exists; raw name shown as secondary text or tooltip",
        "Tree view cluster label (the top-level cluster group header in TreeView) displays the alias when set, raw name otherwise",
        "An edit alias button (pencil icon) appears next to the context name in the AppBar context selector dropdown; clicking it opens an inline input or a small popover with a text field pre-filled with the current alias (or empty if none set)",
        "Saving a non-empty alias writes contextAliases[contextName] = alias to the store; saving an empty string removes the entry (clears the alias, reverts to raw name)",
        "The alias input accepts any string up to 64 characters; no validation beyond length",
        "Aliases persist across app restarts via Zustand persist",
        "All IPC calls, store lookups by context (contextStates, contextNamespaces from S85/S86), and kubeconfig operations continue to use the raw context name — alias is display-only",
        "No TypeScript compile errors"
      ],
      "dependencies": ["S48"],
      "notes": "Store change in app.store.ts; UI change in AppBar context selector (wherever the context dropdown/selector is rendered) and TreeView. The alias edit UI can be a shadcn Popover with an Input and Save/Clear buttons, triggered by a Pencil icon button inline in the context dropdown item. Do not write the alias back to ~/.kube/config — it is app-local metadata stored only in Zustand persist."
    },
    {
      "id": "S88",
      "title": "Apply explicit TypeScript return types to all k8s API handlers",
      "status": "complete",
      "description": "All handler functions in src/main/handlers/ currently have typed parameters but no explicit return types — TypeScript infers them from the mapped object literals. This creates drift risk: callers in main/index.ts rely on inferred shapes that can silently change when a handler is edited. This story introduces a shared types file (src/main/handlers/types.ts) that declares one named interface per handler return shape, then annotates every exported async function with Promise<T> using those interfaces.",
      "acceptance_criteria": [
        "New file src/main/handlers/types.ts exports one TypeScript interface per distinct handler return shape (e.g. NamespaceInfo, NodeInfo, DeploymentInfo, PodInfo, ServiceInfo, IngressInfo, PVInfo, PVCInfo, RoleInfo, ClusterRoleInfo, RoleBindingInfo, ClusterRoleBindingInfo, ConfigMapInfo, SecretInfo, ServiceAccountInfo, EventInfo, JobInfo, CronJobInfo, HPAInfo, ResourceQuotaInfo, LimitRangeInfo, PDBInfo, EndpointInfo, NetworkPolicyInfo, StatefulSetInfo, DaemonSetInfo, ReplicaSetInfo, NodeMetric, ApplyResult)",
        "Every exported async function in all 12 handler files (apply.ts, autoscaling.ts, batch.ts, cluster.ts, config.ts, events.ts, governance.ts, metrics.ts, networking.ts, rbac.ts, storage.ts, workload.ts) is annotated with an explicit Promise<T> or Promise<T[]> return type using only types from types.ts",
        "Mutation handlers that return void or a success flag (delete*, replace*, update*, create*) use a shared MutationResult interface: { success: boolean; error?: string }",
        "No any types introduced; no existing runtime logic changed — only type annotations added",
        "npx tsc --noEmit passes with zero errors after the changes"
      ],
      "dependencies": ["S01"],
      "notes": "Keep types.ts co-located with the handlers. Do not change the shape of objects returned — only add annotations that match the existing inferred shape. If a field is currently typed as string | undefined from optional chaining, reflect that exactly in the interface rather than narrowing to string. The MutationResult pattern should replace any handler that currently returns void or throws on error — wrap the call in try/catch and return { success: true } or { success: false, error: e.message }."
    },
    {
      "id": "S89",
      "title": "Ring buffer for pod log lines in renderer",
      "status": "complete",
      "description": "PodLogPanel.tsx accumulates log lines in unbounded useState<string[]>([]) and useState<MergedLine[]>([]) arrays. For verbose pods, these grow without limit, causing React re-render slowdowns and memory pressure. Cap both arrays at MAX_LOG_LINES = 5000 using a renderer-side ring buffer: on each incoming IPC log chunk, append new lines then slice the tail to MAX_LOG_LINES. No IPC or main-process changes required.",
      "acceptance_criteria": [
        "A constant MAX_LOG_LINES = 5000 is defined at the top of PodLogPanel.tsx (or in a co-located constants file)",
        "The setLines updater always trims: setLines(prev => [...prev, ...incoming].slice(-MAX_LOG_LINES))",
        "The setMergedLines updater applies the same cap: setMergedLines(prev => [...prev, ...newMerged].slice(-MAX_LOG_LINES))",
        "When lines are trimmed, a sentinel string is prepended (or a UI note shown) indicating older lines were dropped, so the user knows the view is not the full history — e.g. the first visible line reads '--- [older lines removed, showing last 5000] ---'",
        "No changes to src/main/ or preload IPC layer — renderer only",
        "No TypeScript compile errors",
        "Existing log features (search/filter, regex mode, merge mode, auto-scroll, container selector) continue to work correctly on the capped line set"
      ],
      "dependencies": ["S88"],
      "notes": "The slice(-MAX_LOG_LINES) approach is O(n) per push but acceptable for 5000-line cap. Do not implement a true circular buffer class — the slice idiom is idiomatic React state and sufficient here. The sentinel line should be a distinct style (muted/italic) if easy, but plain string is acceptable. If mergedLines is built from lines (derived state), the cap on mergedLines may be redundant — verify the data flow in PodLogPanel before deciding whether to cap both or only lines."
    },
    {
      "id": "S90",
      "title": "Local write-action history log",
      "status": "complete",
      "description": "Every write operation (update resource spec via YAML, create resource, delete resource) currently gives no persistent record of what was changed and when. Add a local history log stored in the Zustand persist layer. Each completed write action appends a HistoryEntry to the store. A History panel (new tab or drawer section) lists entries newest-first. No server or filesystem persistence beyond what Zustand persist already does — localStorage only.",
      "acceptance_criteria": [
        "Zustand store gains a persisted field: writeHistory: HistoryEntry[] (capped at 500 entries, oldest dropped when limit exceeded); included in persist partialize",
        "HistoryEntry interface: { id: string; timestamp: string; action: 'create' | 'update' | 'delete' | 'apply'; resourceKind: string; resourceName: string; namespace: string | null; context: string; success: boolean; error?: string; yamlSnapshot?: string }",
        "yamlSnapshot stores the YAML string that was submitted for create/update/apply actions (not for delete); omitted or undefined for delete",
        "Every existing write IPC call site in the renderer appends a HistoryEntry after the call resolves: deployments create/update/delete, statefulsets create/update/delete, daemonsets (if any), services create/update/delete, ingresses create/update/delete, configmaps replace/delete, secrets replace/delete, roles/clusterroles/rolebindings update/delete, PVCs (if any), and the generic apply handler",
        "A History tab or panel is accessible from the main UI (e.g. a 'History' item in the left tree view under a top-level section, or a toolbar button). It renders the writeHistory array in a scrollable list newest-first",
        "Each history row displays: timestamp (local time), action badge (color-coded: green=create, blue=update, red=delete, purple=apply), resourceKind + resourceName, namespace (or cluster-scoped), context, success/fail indicator",
        "Clicking a history row with a yamlSnapshot expands or opens a read-only YAML viewer showing the submitted YAML",
        "A 'Clear history' button empties writeHistory in the store",
        "History persists across app restarts via Zustand persist",
        "No TypeScript compile errors"
      ],
      "dependencies": ["S89"],
      "ipc_handlers": [
        "(no new IPC handlers — history recorded client-side after existing IPC calls resolve)"
      ],
      "notes": "Add the HistoryEntry type to src/main/handlers/types.ts or a renderer-side types file — it is renderer/store data, not IPC data, so a renderer types file is more appropriate. The cap of 500 entries uses the same slice(-500) pattern as the ring buffer story. The yamlSnapshot field lets users review exactly what YAML was applied, useful for rollback reference. Do not implement actual rollback in this story — view only. The History UI can be a simple panel component; no new shadcn components needed beyond what already exists (Badge, ScrollArea or plain overflow-y-auto)."
    },
    {
      "id": "S91",
      "title": "Helm chart management: repos, install, upgrade, uninstall",
      "status": "complete",
      "description": "Add a Helm section to the app that shells out to the `helm` CLI binary via Node.js child_process in the main process. Covers four operations: add or update a repo, list repos, list installed releases, install a chart, upgrade a release, and uninstall a release. All helm commands run with --kubeconfig pointing to the active context's kubeconfig so they operate on the same cluster the rest of the app is connected to. Output (stdout/stderr) is captured and returned to the renderer over IPC.",
      "acceptance_criteria": [
        "Main process checks for `helm` binary on startup (or on first Helm IPC call) using `which helm` / `where helm`; if not found, all helm IPC handlers return { success: false, error: 'helm CLI not found in PATH' }",
        "New IPC handler `helm:repo:add` accepts { name: string; url: string } and runs `helm repo add <name> <url> --force-update`; returns MutationResult",
        "New IPC handler `helm:repo:list` runs `helm repo list -o json`; returns parsed array of { name: string; url: string } or empty array if no repos",
        "New IPC handler `helm:release:list` accepts { namespace?: string } and runs `helm list -o json [-n <namespace> | -A]`; returns array of HelmRelease: { name: string; namespace: string; chart: string; chartVersion: string; appVersion: string; status: string; updated: string }",
        "New IPC handler `helm:release:install` accepts { releaseName: string; chart: string; namespace: string; values?: string }; runs `helm install <releaseName> <chart> -n <namespace> [--values -]` (piping values YAML to stdin if provided); returns MutationResult with stdout on success",
        "New IPC handler `helm:release:upgrade` accepts { releaseName: string; chart: string; namespace: string; values?: string }; runs `helm upgrade <releaseName> <chart> -n <namespace> --install [--values -]`; returns MutationResult with stdout on success",
        "New IPC handler `helm:release:uninstall` accepts { releaseName: string; namespace: string }; runs `helm uninstall <releaseName> -n <namespace>`; returns MutationResult",
        "A 'Helm' section appears in the left tree view with two sub-items: 'Repositories' and 'Releases'",
        "Repositories panel: table of repos (name, URL) with an 'Add / Update Repo' button that opens a dialog with name + URL inputs; submits to helm:repo:add",
        "Releases panel: table of all releases across all namespaces (helm:release:list with -A); columns: name, namespace, chart, version, status, last updated; per-row actions: Upgrade (opens dialog pre-filled with release name + chart, editable values YAML textarea) and Uninstall (confirmation dialog)",
        "Install chart button on Releases panel opens a dialog: release name, chart (e.g. bitnami/nginx), namespace selector, optional values YAML textarea; submits to helm:release:install",
        "All helm operations append a HistoryEntry to writeHistory (action: 'create' for install, 'update' for upgrade, 'delete' for uninstall, resourceKind: 'HelmRelease')",
        "No TypeScript compile errors"
      ],
      "dependencies": ["S90"],
      "ipc_handlers": [
        "helm:repo:add",
        "helm:repo:list",
        "helm:release:list",
        "helm:release:install",
        "helm:release:upgrade",
        "helm:release:uninstall"
      ],
      "notes": "All helm CLI calls use execFile (not exec) with an explicit args array to avoid shell injection. Pass KUBECONFIG env var or --kube-context flag so helm targets the active context. Values YAML is passed via stdin (write to child process stdin and close) not via a temp file. Helm IPC handlers live in a new src/main/handlers/helm.ts file. No npm packages needed — only the system helm binary. If helm list returns an empty array (no releases), return [] not an error. Timeout each helm spawn at 30 seconds."
    }
  ]
}
